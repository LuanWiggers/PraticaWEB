function mediaSalario(){
    let continuar = false;
    
    

    while(continuar!="finalizar"){
        let idade = parseInt(prompt("Digite sua idade:"));
        let genero = parseFloat(prompt("Digite o gênero:"));
        let salario = parseInt(prompt("Digite seu salário"));

        


        if (resposta.toLowerCase() !== 'finalizar') {
            continuar = true;
            alert("Programa encerrado.");
        }

        
        }
    }
